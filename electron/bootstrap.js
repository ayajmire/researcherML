/**
 * Bootstrap loader for ResearcherML Electron App
 * Handles Electron module loading edge cases on macOS
 */

// Simply load the main file - Electron will handle module resolution
require('./main.js');
